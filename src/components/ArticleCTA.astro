---
---
<section class="article-cta" id="talk-to-ai">
  <div class="cta-inner">
    <h2>Ready to See AI in Action?</h2>
    <p>Talk to our AI concierge right now. Ask about automation solutions for your business  no forms, no wait times, just answers.</p>
    <button class="cta-button" onclick="startAICall()">Talk to Our AI Concierge</button>
    <p class="cta-sub">Free consultation &bull; No commitment &bull; Available 24/7</p>
  </div>
</section>

<style>
.article-cta {
  margin: 3rem 0 2rem;
  padding: 3rem 2rem;
  background: linear-gradient(135deg, rgba(99,102,241,0.1), rgba(168,85,247,0.1));
  border-radius: 16px;
  text-align: center;
  border: 1px solid var(--color-border);
}
.cta-inner { max-width: 600px; margin: 0 auto; }
.article-cta h2 {
  font-size: 1.75rem;
  margin-bottom: 1rem;
  color: var(--color-text-heading);
}
.article-cta p {
  color: var(--color-text-muted);
  margin-bottom: 1.5rem;
  line-height: 1.6;
}
.cta-button {
  display: inline-block;
  padding: 1rem 2.5rem;
  background: linear-gradient(135deg, #6366f1, #a855f7);
  color: white;
  border: none;
  border-radius: 12px;
  font-size: 1.1rem;
  font-weight: 600;
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s;
}
.cta-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(99,102,241,0.3);
}
.cta-sub {
  font-size: 0.85rem !important;
  margin-top: 1rem !important;
}
</style>
